- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.shell: 
    ~/.vim_cfg:
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.bashrc: bashrc
    ~/.zephyrrc: zephyrrc
    ~/.ide:
    ~/.arm:
    ~/.zsh:
    ~/.bash:
    ~/.tmux.conf: tmux.conf
    ~/.gitignore_global:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
